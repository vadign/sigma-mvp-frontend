# Сигма — демо стенд

Одностраничное приложение на React/Vite для демонстрации возможностей системы «Сигма».

## Скрипты
- `npm install` — установка зависимостей
- `npm run dev` — запуск стенда в dev-режиме
- `npm run build` — сборка продакшн-версии
